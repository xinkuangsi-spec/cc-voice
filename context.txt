# 识别上下文：写你是谁、在做什么、平时怎么说话。每次识别都会交给 Qwen3-ASR 当背景参考，
# 它会照着这里的话题和用词去猜同音字、专有名词。写背景和词，不写指令。
# `#` 后面是注释，不会发给模型。改完在管理面板点「保存设置」即时生效。

说话人是在 Windows 上写代码的中文程序员，普通话，中英混说，英文词多是工具名和编程术语。
平时主要在对 AI 编程助手下指令：让它读文件、改代码、跑命令、提交推送、查报错。
常用 Claude Code（cc、cc2、cc3 三个入口）、Codex、Devin、Orca、herdr，终端是 PowerShell。
在做的项目：ComfyUI 工作流和节点、本地语音输入 cc-voice、项目大脑知识库。
本地模型用 llama.cpp、Ollama、Qwen，模型放在 D 盘的 models 目录。
